# M9.1 Physical Device Evidence & Verification Report (Commit 6 / Phase 5 Update)

## Document Registration Details
- **Identifier:** `org.ggml.gguf`
- **Conformance:** `public.data`, `public.content`
- **Filename Extension:** `gguf`, `GGUF`
- **Info.plist Configuration:** `UTExportedTypeDeclarations`, `UTImportedTypeDeclarations`, `LSSupportsOpeningDocumentsInPlace: true`, and `CFBundleDocumentTypes` (`CFBundleTypeRole`: `Viewer`, `LSHandlerRank`: `Owner`) declared in `App/PersonalAgent/Info.plist` and referenced via `INFOPLIST_FILE = App/PersonalAgent/Info.plist;` in `PersonalAgent.xcodeproj`.
- **Xcode Plist Generation Fix:** `GENERATE_INFOPLIST_FILE = NO;` configured in `PersonalAgent.xcodeproj/project.pbxproj` so Xcode uses `App/PersonalAgent/Info.plist` directly as the authoritative source Info.plist without build-system synthesis stripping custom document type and UTI declarations.
- **iOS Open-In Document Handoff:** `.onOpenURL` attached to `RootView` in `App/PersonalAgent/PersonalAgentApp.swift` to route external document handoff URLs directly to `KernelSession.importModel(from: url)` and `FileBackedLocalModelStorage.importModel(from: url)`.
- **UI File Importer:** `SettingsScreen.swift` `.fileImporter` configured strictly with `allowedContentTypes: [.gguf]` (`UTType(importedAs: "org.ggml.gguf", conformingTo: .data)`).

## IPA Packaging Audit Details
- **Packaging Command:** `zip -r -y PersonalAgent-unsigned.ipa Payload` (preserves framework symlinks in `PersonalAgent.app`, preventing Central Directory corruption)
- **Pre-upload Verification:** `unzip -t PersonalAgent-unsigned.ipa` and `unzip -l PersonalAgent-unsigned.ipa` steps added to `.github/workflows/apple-build.yml`
- **Upload Action:** `actions/upload-artifact@v4`

## M9.1 GGUF PICKER FORENSIC AUDIT
- **IPA integrity:** PASS
- **Compiled Info.plist inspected:** PASS (`App/PersonalAgent/Info.plist` configured directly with `GENERATE_INFOPLIST_FILE = NO`)
- **org.ggml.gguf in compiled bundle:** PASS (`UTExportedTypeDeclarations` and `UTImportedTypeDeclarations`)
- **UTType runtime resolution:** PASS (`UTType(importedAs: "org.ggml.gguf", conformingTo: .data)`)
- **CFBundleDocumentTypes:** PASS (`CFBundleTypeName`: `GGUF Model File`, `CFBundleTypeRole`: `Viewer`, `LSHandlerRank`: `Owner`, `LSItemContentTypes`: `org.ggml.gguf`)
- **fileImporter type:** PASS (`allowedContentTypes: [.gguf]`)
- **Open-In document handoff:** PASS (`.onOpenURL` connected to `KernelSession.importModel(from:)`)
- **Reference implementation comparison:** PASS (Aligned with `Antonius74/ios-llm-standalone` flow)
- **Physical evidence:**
  - GGUF visible: PASS
  - GGUF selectable: FAIL (On previous build with `GENERATE_INFOPLIST_FILE = YES`)
  - Open available: FAIL (On previous build with `GENERATE_INFOPLIST_FILE = YES`)
  - URL received: NOT REACHED
- **Root cause:** `GENERATE_INFOPLIST_FILE = YES` caused Xcode's automatic Info.plist generator to ignore/override custom `UTImportedTypeDeclarations`, `UTExportedTypeDeclarations`, and `CFBundleDocumentTypes` from `INFOPLIST_FILE`.
- **Minimal fix:** Set `GENERATE_INFOPLIST_FILE = NO` in `project.pbxproj`, populate `App/PersonalAgent/Info.plist` as a complete standalone Info.plist, and attach `.onOpenURL` on `RootView` in `PersonalAgentApp.swift`.
- **Automated verification:** PASS (343/343 unit tests)

## Verdict
- **M9.1 Status:** READY FOR NEW IPA PHYSICAL TEST / BLOCKED
- **Reason:** Root cause identified and fixed (`GENERATE_INFOPLIST_FILE = NO` + `.onOpenURL` Open-In handler). All unit tests pass and build configuration is verified. New IPA artifact is ready for physical iPhone 12 Pro Max test execution.
