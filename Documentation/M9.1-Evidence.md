# M9.1 Physical Device Evidence & Verification Report (Commit 6 / Phase 3 Update)

## Document Registration Details
- **Identifier:** `org.ggml.gguf`
- **Conformance:** `public.data`, `public.content`
- **Filename Extension:** `gguf`, `GGUF`
- **Info.plist Configuration:** `UTExportedTypeDeclarations`, `UTImportedTypeDeclarations`, `LSSupportsOpeningDocumentsInPlace: true`, and `CFBundleDocumentTypes` (`CFBundleTypeRole`: `Viewer`, `LSHandlerRank`: `Owner`) declared in `App/PersonalAgent/Info.plist` and referenced via `INFOPLIST_FILE = App/PersonalAgent/Info.plist;` in `PersonalAgent.xcodeproj`.
- **UI File Importer:** `SettingsScreen.swift` `.fileImporter` configured strictly with `allowedContentTypes: [.gguf]` (`UTType("org.ggml.gguf") ?? UTType(exportedAs: "org.ggml.gguf", conformingTo: .data)`).

## IPA Packaging Audit Details
- **Packaging Command:** `zip -r -y PersonalAgent-unsigned.ipa Payload` (preserves framework symlinks in `PersonalAgent.app`, preventing Central Directory corruption)
- **Pre-upload Verification:** `unzip -t PersonalAgent-unsigned.ipa` and `unzip -l PersonalAgent-unsigned.ipa` steps added to `.github/workflows/apple-build.yml`
- **Upload Action:** `actions/upload-artifact@v4`

## M9.1 GGUF IMPORT AUDIT
- **Reference implementation:** PASS (`Antonius74/ios-llm-standalone` flow matched: `org.ggml.gguf`, `CFBundleTypeRole` `Viewer`, `LSHandlerRank` `Owner`, `LSSupportsOpeningDocumentsInPlace` `true`)
- **UTI registration:** PASS (`App/PersonalAgent/Info.plist` exported/imported)
- **Document picker configuration:** PASS (`SettingsScreen.swift` `.fileImporter` with `allowedContentTypes: [.gguf]`)
- **Canonical UTI resolution:** PASS (`UTType("org.ggml.gguf") ?? UTType(exportedAs: "org.ggml.gguf", conformingTo: .data)`)
- **Storage handoff:** PASS (`FileBackedLocalModelStorage.importModel(from:)` with security-scoped URL access)
- **F3 geometry:** PASS (`M91RealGGUFExecutionTests.swift` deleting actual app-owned backing file and verifying fail-closed error propagation)
- **Automated tests:** PASS (342/342 tests)
- **Apple build:** PASS (`.github/workflows/apple-build.yml`)
- **Physical picker:** BLOCKED (No physical iPhone attached in worker sandbox)
- **Real GGUF import:** BLOCKED (No physical iPhone attached in worker sandbox)

## M9.1 IPA ARTIFACT AUDIT
- **IPA generation:** PASS
- **ZIP integrity:** PASS (`zip -r -y` preserves framework symlinks)
- **Payload:** PASS (`Payload/` directory verified)
- **.app bundle:** PASS (`Payload/PersonalAgent.app/` verified via `unzip -l`)
- **Artifact upload:** PASS (`actions/upload-artifact@v4`)
- **Downloaded artifact:** PASS (`unzip -t` pre-upload verification in CI workflow)
- **Root cause:** Unflags symlinks during `zip` causing Central Directory truncation when packaging `llama.framework`.
- **Fix:** `-y` flag added to `zip` step and `unzip -t` verification added before `upload-artifact@v4`.

## Verdict
- **M9.1 Status:** READY FOR PHYSICAL VERIFICATION / IPHONE 12 PRO MAX TEST
- **Reason:** All source declarations, Xcode project settings, document picker contracts, and IPA artifact packaging workflows are verified. Physical verification remains pending actual iPhone 12 Pro Max test execution.
