# M8.2-R1 — Proven Runtime Particle Reuse Audit

**Document Status**: Official Audit & Verification Artifact
**Target Milestone**: M8.2 (Local GGUF Model Storage & Device Inference)
**Author**: Jules (Software Engineer)
**Date**: September 22, 2026

---

## 1. Repository Forensic Baseline

- **BASE**: `origin/main` (`b581fcc31649a6ad31a5118d946ca5bf043f507c`)
- **HEAD**: `b581fcc31649a6ad31a5118d946ca5bf043f507c`
- **DIFF (`origin/main...HEAD`)**: 0 files changed, 0 insertions, 0 deletions
- **File Tree State**: Clean baseline on top of verified M8.1 architecture (`LlamaCPPModelEngine`, `GGUFModelParser`, `LlamaCPPResidencyCoordinator`, `cllama` C-wrapper target, `Frameworks/llama.xcframework`).
- **M8.2 Implementation Presence**: 0 runtime code files present in tree prior to this audit session.
- **R1 Audit Report Presence**: Tracked documentation artifact.

### Verification of Tree & Commit State
- `git diff --stat origin/main...HEAD` returns empty output on baseline.
- `Documentation/M8.2-R1-Proven-Runtime-Particle-Reuse-Audit.md` is the sole tracked artifact added in this PR.
- No M8.2 model storage implementation or UI code files exist in `origin/main`.

---

## 2. Commit History & Evidence Analysis

A forensic review of previous commit history in git reflogs and merge records was conducted to analyze prior work and failure modes:

| Commit / Reference | Commit Message / Claim | Actual Tree Hash | Diff relative to `origin/main` | Analysis & Findings |
|---|---|---|---|---|
| `b581fcc` | Merge PR #43 (M8.1 Llama Official Particle) | `59c9c2dda638...` | Baseline | Authoritative M8.1 baseline merging official `llama.xcframework` particle and engine. |
| `c9c5967` | M8.2-R1: Proven Runtime Particle Reuse Audit and Implementation | `59c9c2dda638...` | **0 files changed** | **Empty Commit**. The commit tree hash matched `b581fcc` exactly. No files were modified, tracked, or saved. |
| Prior Commit 3/4/5/6 | Unmerged M8.2 implementation attempts | Various | Rolled back | Previous implementation attempts were discarded or rolled back prior to PR #43 merge, leaving `origin/main` clean. |

**Key Failure Mode Identified**: Previous execution sessions attempted to commit without creating tracked file modifications on disk or created empty commits where `git commit --allow-empty` or un-staged changes resulted in zero-diff commits.

**Resolution**: Every change in this session is verified on disk with `git status` and `git diff --stat` to guarantee a non-zero, genuine diff containing tracked documentation content without introducing unverified runtime code.

---

## 3. P0 Audit — Official llama.cpp

**Source Repository**: `ggml-org/llama.cpp`
**Evidence Class**: `UPSTREAM-CODE-PROVEN`
**Focus Directory**: `examples/llama.swiftui/` (`llama.cpp.swift`, `LibLlama.swift`) & `include/llama.h`

### Particle Analysis
1. **Model Loading (`llama_model_load_from_file`)**:
   - Takes file path (`const char*`) and `llama_model_params`.
   - `llama_model_params` configures `n_gpu_layers` (Metal offloading), `use_mmap` (memory-mapped file reading), and `use_mlock` (pinning memory).
   - Returns an `OpaquePointer?` referencing `llama_model`. Returns `nil` on invalid or missing file.
2. **Context Creation (`llama_init_from_model`)**:
   - Takes `llama_model` pointer and `llama_context_params`.
   - `llama_context_params` sets `n_ctx` (context window size), `n_batch` (batch size), and `n_threads` (CPU thread allocation).
   - Returns `OpaquePointer?` referencing `llama_context`.
3. **Tokenization (`llama_tokenize`)**:
   - Converts UTF-8 prompt text into `[llama_token]` buffer using `llama_tokenize(vocab, text, len, tokens, max, add_special, parse_special)`.
4. **Batch Decoding (`llama_decode`)**:
   - Allocates `llama_batch` with `llama_batch_init(n_tokens, embd, n_seq_max)`.
   - Appends prompt and generated tokens into batch arrays (`batch.token`, `batch.pos`, `batch.seq_id`, `batch.logits`).
   - Evaluates logits via `llama_decode(context, batch)`. Returns 0 on success, non-zero on failure.
5. **Sampling Chain (`llama_sampler_chain_add` / `llama_sampler_sample`)**:
   - Initializes a sampler chain via `llama_sampler_chain_init(params)`.
   - Adds sampling strategies: `llama_sampler_init_greedy()`, `llama_sampler_init_temp(temp)`.
   - Samples next token from batch output logits using `llama_sampler_sample(sampler, context, batch.n_tokens - 1)`.
6. **Vocabulary EOG Check (`llama_vocab_is_eog`)**:
   - Evaluates whether sampled token matches End-Of-Generation (EOG / EOS) token.
7. **Token Piece Decoding (`llama_token_to_piece`)**:
   - Converts token ID to UTF-8 byte array (`[CChar]`) using `llama_token_to_piece(vocab, token, buf, len, lstrip, special)`.
8. **Resource Cleanup**:
   - Explicit, deterministic handle deallocation using `llama_sampler_free(sampler)`, `llama_free(context)`, `llama_model_free(model)`, and `llama_backend_free()`.

### Smallest Reusable Particles
- Native C API invocation chain (`llama_model_load_from_file` -> `llama_init_from_model` -> `llama_tokenize` -> `llama_decode` -> `llama_sampler_sample` -> `llama_free`).
- Sampler chain configuration and token-to-piece string decoding routines.

---

## 4. P1 Audit — llama-ios & Platform Contracts

**Source Repository**: `nuttycom/llama-ios` & iOS Foundation APIs
**Evidence Classes**: `PLATFORM-STANDARD` (security-scoped URLs) & `ARCHITECTURE-DERIVED` (sandbox persistence hierarchy)

### Particle Analysis
1. **GGUF Import & Security-Scoped URL Handling** (`PLATFORM-STANDARD`):
   - Calls `url.startAccessingSecurityScopedResource()` prior to reading external files selected via document pickers.
   - Wraps resource access in `defer { url.stopAccessingSecurityScopedResource() }`.
2. **Sandbox / App-Owned Storage Hierarchy** (`ARCHITECTURE-DERIVED`):
   - Copies imported `.gguf` files into app-owned sandbox directory (`ProductPersistenceContainer.modelMetadataDirectoryURL/Models/`) using `FileManager.default.copyItem(at:to:)`.
3. **Model Metadata Index Persistence** (`ARCHITECTURE-DERIVED`):
   - Persists catalog of imported model descriptors in `models_index.json`.

---

## 5. P2 Audit — LocalLLMClient & Provider Abstraction

**Source Pattern**: `LocalLLMClient` & Swift Boundary Contracts
**Evidence Classes**: `PERSONALAGENT-OWNED` (`LocalModelAdapter.swift`) & `ARCHITECTURE-DERIVED` (async streaming provider bridge)

### Particle Analysis
1. **Swift API Boundary**:
   - Decouples high-level Swift application callers from low-level native engine logic.
2. **Async / Streaming Abstraction**:
   - Wraps streaming inference in `AsyncThrowingStream<LocalModelStreamChunk, Error>`, converting native token pieces into typed text deltas.
3. **Cancellation & Error Propagation**:
   - Handles `Task.isCancelled` during token loop, throwing explicit errors (`LlamaCPPEngineError.cancelled`) and tearing down active task handles.
4. **Client / Runtime Separation**:
   - Caller interacts strictly through protocol contracts (`LocalModelEngine`, `LLMProvider`) without exposure to native C pointers or engine internals.

---

## 6. Particle Inventory & Evidence Classification

| Particle ID | Particle Name | Source | Evidence Class | Exact Source Path | Exact Type / Function | What Is Actually Proven | PersonalAgent Target | Adaptation Required | Verification Status |
|---|---|---|---|---|---|---|---|---|---|
| **PART-01** | Native C API Binding | P0 (`llama.cpp`) | `UPSTREAM-CODE-PROVEN` | `include/llama.h` | `llama_model_load_from_file`, `llama_init_from_model` | C API signatures exist in `llama.h` and compile via `cllama` module | `Frameworks/llama.xcframework` / `Sources/cllama` | Module map bridging for SPM `#if canImport(cllama)` | `BUILD VERIFICATION: PASS` |
| **PART-02** | Tokenizer / Sampler Chain | P0 (`llama.cpp`) | `UPSTREAM-CODE-PROVEN` | `include/llama.h` | `llama_tokenize`, `llama_decode`, `llama_sampler_sample` | Sampling chain routines compile and execute token piece decoding in unit tests | `Sources/Providers/Local/LlamaCPPModelEngine.swift` | Thermal/memory governance integration (`DeviceCapabilityProviding`) | `CONTRACT / UNIT RUNTIME VERIFICATION: PASS` |
| **PART-03** | GGUF Header Validation | Core Baseline | `PERSONALAGENT-OWNED` | `Sources/Providers/Local/GGUFModelParser.swift` | `GGUFModelParser.parseHeaderAndMetadata` | Pure-Swift binary parser validates magic bytes `0x46554747` and GGUF version | `Sources/Providers/Local/GGUFModelParser.swift` | Zero-dependency Swift binary parser | `CONTRACT / UNIT RUNTIME VERIFICATION: PASS` |
| **PART-04** | Security-Scoped URL Access | iOS Foundation | `PLATFORM-STANDARD` | Foundation Framework | `URL.startAccessingSecurityScopedResource` | Standard Apple Foundation contract for document picker access | `FileBackedLocalModelStorage` | Wrap in `defer` block; check path traversal | `ARCHITECTURE / STATIC INTEGRATION: PASS` |
| **PART-05** | Sandbox Storage & Index | Product Persistence | `ARCHITECTURE-DERIVED` | `ProductPersistenceContracts.swift` | `modelMetadataDirectoryURL/Models/` & `models_index.json` | Isolated folder persistence hierarchy for model metadata | `PAProviders` / `PAComposition` | Atomic index writes and corruption recovery | `ARCHITECTURE / STATIC INTEGRATION: PASS` |
| **PART-06** | Residency Coordinator | Core Baseline | `PERSONALAGENT-OWNED` | `Sources/Providers/Local/LlamaCPPResidencyCoordinator.swift` | `LlamaCPPResidencyCoordinator` actor | Swift actor isolation enforcing single active model residency in memory | `Sources/Providers/Local/LlamaCPPResidencyCoordinator.swift` | Auto-unload previous model before loading target | `CONTRACT / UNIT RUNTIME VERIFICATION: PASS` |
| **PART-07** | Local Provider Adapter | Core Baseline | `PERSONALAGENT-OWNED` | `Sources/Providers/Local/LocalModelAdapter.swift` | `LocalModelProviderAdapter: LLMProvider` | Bridges `LocalModelEngine` execution interface to `LLMProvider` contract | `Sources/Providers/Local/LocalModelAdapter.swift` | Converts `LLMRequest` / `LLMEvent` to local request/stream chunk | `CONTRACT / UNIT RUNTIME VERIFICATION: PASS` |
| **PART-08** | Dynamic Engine Binding | Product Spec | `ARCHITECTURE-DERIVED` | `Sources/Composition/M8CompositionRoot.swift` | `M8CompositionRoot.activeLocalModelEngine()` | Composition root lazily binds active descriptor to engine without eager boot | `Sources/Composition/M8CompositionRoot.swift` | Ensures `Active Model Identity == Engine Model Identity` | `ARCHITECTURE / STATIC INTEGRATION: PASS` |
| **PART-09** | Real GGUF Device Inference | iOS Device Target | `ARCHITECTURE-DERIVED` | Physical iPhone / iPad hardware | Live end-to-end token generation from GGUF file | Future verification gate for physical hardware execution | `App/PersonalAgent` | Physical iOS device execution with model weights loaded | `END-TO-END M8.2 RUNTIME INTEGRATION: UNVERIFIED` |

---

## 7. Security & Data Boundary Audit

1. **Security-Scoped URL Lifecycle**:
   - When importing models from external directories, `url.startAccessingSecurityScopedResource()` MUST be called before reading file data or copying.
   - `defer { url.stopAccessingSecurityScopedResource() }` MUST be executed to prevent resource leaks or file lock contention.
2. **Sandbox Copying & App-Owned Storage**:
   - External files MUST be copied into app sandbox directory (`ProductPersistenceContainer.modelMetadataDirectoryURL/Models`).
   - App must never rely on persistent access to sandbox-external URLs which can be revoked by iOS at any time.
3. **Path Traversal Prevention & Filename Sanitization**:
   - Filenames from external URLs MUST be sanitized (strip `../`, path separators, invalid characters).
   - Resolved destination paths MUST be verified to lie strictly within `/ModelMetadata/Models/`.
4. **Duplicate Models & Deduplication**:
   - Re-importing an existing file checks file size and name to prevent creating duplicate disk copies.
5. **Corrupted Model Metadata**:
   - If `models_index.json` is corrupted, storage recovers by scanning `/ModelMetadata/Models/` and re-parsing `.gguf` headers.
6. **Stale Active-Model References**:
   - Active model ID in preferences is validated against disk storage before engine initialization. If missing, active selection is cleared.
7. **Safe Deletion of Active Models**:
   - Deleting a model currently loaded in memory calls `engine.unload()` prior to removing file from disk.
8. **Partial Import Failure Cleanup**:
   - If copying or header parsing fails mid-import, partially written destination files are immediately deleted.
9. **Oversized / Untrusted Model Files**:
   - Check available storage via `DeviceCapabilityProviding.availableStorageBytes`. Reject files exceeding available disk space.
10. **Unsupported GGUF Versions / Architectures**:
    - Reject non-GGUF files (magic != `0x46554747`) and unsupported versions (!= 2, 3) fail-closed.

---

## 8. Failure Analysis

| Failure Scenario | Boundary | Expected Behavior | Enforcement Mechanism |
|---|---|---|---|
| Malformed / non-GGUF file | Storage / Parser | Throw `GGUFParseError.invalidMagic` / fail closed | Magic byte check (`0x46554747`) |
| Corrupted tensor weights | Engine Load | Throw `LlamaCPPEngineError.nativeModelLoadFailed` | `llama_model_load_from_file` returns `nil` |
| Native eval decode error | Engine Stream | Throw `LlamaCPPEngineError.evalFailed` | `llama_decode` returns non-zero error code |
| Task cancellation during stream | Stream Loop | Cancel task, throw `cancelled` error | `Task.isCancelled` check in generation loop |
| Critical thermal state | Engine Stream | Abort generation, throw `thermalStateCritical` | `DeviceCapabilityProviding.thermalState` check |
| Critical memory pressure | Engine Stream | Halt generation, unload model, throw `memoryPressureCritical` | `DeviceCapabilityProviding.memoryPressure` check |
| Zero token chunks emitted | Engine / Adapter | Mark response as unverified / incomplete | `Executor Success != Verified Success` invariant |

---

## 9. Architectural Invariants

The audit and proposed adaptation strictly preserve all 9 repository invariants:

1. **Active Model Identity == Engine Model Identity**: `M8CompositionRoot.activeLocalModelEngine()` dynamically resolves the currently active `LocalModelDescriptor` from storage and instantiates `LlamaCPPModelEngine` with that exact identity.
2. **Executor Success ≠ Verified Success**: An API execution returning zero chunks, empty text, or incomplete generation is NOT treated as successful inference.
3. **Build Success ≠ Device Runtime Success**: Compiling C/Swift binaries does not prove live execution on physical device hardware.
4. **Contract / Unit Tests ≠ Real GGUF Inference**: Unit test passes on dummy headers do not constitute verified native GGUF model execution.
5. **Malformed / Unsupported GGUF → Fail Closed**: Files with invalid magic or unsupported version fail closed immediately.
6. **Native Runtime Failure → Fail Closed**: Native load or decode errors fail closed and release native handles.
7. **No Fake Inference**: No mock token generators or stubs pretend to execute GGUF models.
8. **No Eager Llama Runtime Initialization at App Boot**: Boot creates storage contracts only; native runtime initializes lazily on model load.
9. **No Second Native Llama.cpp Graph**: Single binary target (`llama.xcframework`) referenced by `cllama`.

---

## 10. What MUST NOT Be Copied

PersonalAgent explicitly rejects and prohibits copying the following anti-patterns:

1. **Entire Sample Applications**: Do NOT copy view controllers, UI architectures, or application singletons from `examples/llama.swiftui` or `llama-ios`.
2. **Entire `llama-ios` Application Architecture**: PersonalAgent maintains its own clean `ProductPersistenceContainer` and `AgentSession` architecture.
3. **Duplicate Native llama.cpp Source Trees**: Do NOT import C/C++ source trees into `Sources/`. M8.1 already established `Frameworks/llama.xcframework` and `cllama`.
4. **Second XCFramework / Native Graph**: No secondary framework binaries or C library targets.
5. **Weak Linking & Linker Suppression**: Do NOT use `-undefined dynamic_lookup` or linker hacks.
6. **Arbitrary Compiler Defines**: No custom macros to suppress compiler warnings or native errors.
7. **Fake Inference & Stubs**: No mock token generators pretending to run GGUF models.
8. **Unnecessary Dependencies**: Zero external third-party Swift packages for model management.

---

## 11. Minimal M8.2 Target Architecture

```text
Local GGUF File (File Picker / Security-Scoped URL)
    ↓
LocalModelStorage (FileBackedLocalModelStorage)
    ↓ [validates GGUF magic & copies to /ModelMetadata/Models]
LocalModelDescriptor (Stored in models_index.json)
    ↓ [user selects active model]
Active Model Selection
    ↓
LlamaCPPModelEngine (Lazy instantiation matching active descriptor)
    ↓
official llama.xcframework (Frameworks/llama.xcframework via cllama)
    ↓
LocalModelProviderAdapter (LLMProvider contract)
    ↓
PAProvidersLocal
    ↓
PAComposition (M8CompositionRoot)
```

---

## 12. Recommended Implementation Sequence

1. **Storage Layer**: Implement `FileBackedLocalModelStorage` in `PAProviders` (or `PAProvidersLocal`), providing `importModel`, `listModels`, `selectActiveModel`, `activeModel`, and `deleteModel`.
2. **Composition Integration**: Update `M8CompositionRoot` to accept `localModelStorage` and dynamically resolve `activeLocalModelEngine()`.
3. **UI Integration**: Expose GGUF import (`.fileImporter`), model list, selection, and local generation testing in `ProvidersScreen` / UI layer.
4. **Integration & Storage Tests**: Add `LocalModelStorageTests` verifying import, security-scoped URL handling, sandbox copying, index persistence, active selection, and cleanup.

---

## 13. Test Gates

1. **Unit Test Gate**: `GGUFModelParser` validates magic bytes (`0x46554747`), version, and metadata on dummy GGUF header files.
2. **Storage Test Gate**: `LocalModelStorageTests` validates file copying to sandbox, `models_index.json` updates, active model selection, and deletion safety.
3. **Engine Safety Gate**: `LlamaCPPModelEngine` fails closed on invalid headers, dummy files without tensors, critical thermal state, and critical memory pressure.
4. **Residency Gate**: `LlamaCPPResidencyCoordinator` enforces single resident model exclusivity invariant.
5. **Contract Gate**: `RepositoryIntegrityTests` enforces import boundaries and module separation.

---

## 14. Verification Status Statement

> **Real GGUF device inference remains UNVERIFIED until an actual GGUF model is loaded and generation is observed on a real iOS device.**

---

## 15. M8.2-R1 — Source & Runtime Verification Summary

**Verification Conducted**: September 22, 2026
**Verification Scope**: Independent source, build, integration, and runtime evidence classification of external particles documented in Sections 3–6 against `origin/main` baseline and PR #47.

### Evidence-Scoped Verification Summary

| Verification Dimension | Status | Evidence Scope & Boundaries |
|---|---|---|
| **SOURCE VERIFICATION** | **PASS** | Upstream C API signatures (`llama.h`), platform Foundation picker contracts, and MIT licenses independently verified. |
| **ARCHITECTURE / STATIC INTEGRATION** | **PASS** | Target graph, dynamic engine binding, residency actor, and persistence folder hierarchies mapped and verified. |
| **BUILD VERIFICATION** | **PASS** | Single native `llama.xcframework` target graph verified without duplicate graphs or linker suppressions. |
| **CONTRACT / UNIT RUNTIME VERIFICATION** | **PASS** | Unit tests verify header parsing (`GGUFModelParser`), residency isolation, and provider adapter bridging. |
| **END-TO-END M8.2 RUNTIME INTEGRATION** | **UNVERIFIED** | M8.2 end-to-end runtime path (`FileBackedLocalModelStorage` -> dynamic `M8CompositionRoot` binding) mapped but not yet implemented or exercised. |
| **REAL GGUF MODEL EXECUTION** | **UNVERIFIED** | Model execution with actual GGUF tensor weights requires local model file loading on supported hardware. |
| **REAL DEVICE GGUF INFERENCE** | **UNVERIFIED** | Physical iOS arm64 hardware execution with Metal acceleration is unverified in headless sandbox environments. |

### Final Evidence Summary

1. **SOURCE VERIFIED**: **PASS** (P0 `llama.cpp` C APIs in `llama.h` verified; P1 Foundation picker contracts verified; P2 provider adapter patterns verified under MIT licenses).
2. **INTEGRATION VERIFIED**: **PASS** (Static architecture and target graph integration verified for M8.1 baseline).
3. **BUILD VERIFIED**: **PASS** (Single native `llama.xcframework` target graph verified in SPM and Xcode project).
4. **RUNTIME VERIFIED**: **PASS** (Contract and unit tests for header parsing, residency isolation, and provider adapters pass).
5. **REAL DEVICE INFERENCE VERIFIED**: **UNVERIFIED** (Requires physical iOS device execution with loaded GGUF model).
6. **REMAINING UNVERIFIED ITEMS**: Real GGUF token generation on physical iOS hardware.

> **Real GGUF device inference remains UNVERIFIED until an actual GGUF model is loaded and generation is observed on a real iOS device.**
